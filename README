## 开发

### 添加新包
创建子文件夹，添加 package.json

```bash
pnpm install xxx --filter @yikyo/xxx
```

### 更新版本

```bash
pnpm changeset add
```

提交相关代码，合并到主分支后，Github Actions 会自动发布新版本。

````bash

## 使用

### 使用 eslint

```bash
pnpm install -D @yikyo/eslint-config-nestjs
````

.eslintrc.cjs

```cjs
module.exports = {
  extends: '@yikyo/eslint-config-nestjs',
};
```

### 使用 prettier

```bash
pnpm install -D @yikyo/prettier-config-standard
```

.prettierrc.cjs

```cjs
module.exports = '@yikyo/prettier-config-standard';
```